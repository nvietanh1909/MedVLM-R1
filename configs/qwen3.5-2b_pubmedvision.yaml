model:
  name: qwen3.5-2b_pubmedvision
  model_path: /data2/cmdir/home/ioit_thql/ducanhUET/finetune/models/Qwen3.5-2B
  load_in_4bit: false
  use_gradient_checkpointing: "unsloth"

lora:
  finetune_vision_layers: true
  finetune_language_layers: true
  finetune_attention_modules: true
  finetune_mlp_modules: true
  r: 16
  lora_alpha: 16
  lora_dropout: 0
  bias: none
  random_state: 3407
  use_rslora: false

dataset:
  type: pubmedvision
  data_path: /data2/cmdir/home/ioit_thql/ducanhUET/finetune/dataset/PubMedVision
  json_file: PubMedVision_InstructionTuning_VQA.json
  max_samples: null

training:
  output_dir: /data2/cmdir/home/ioit_thql/ducanhUET/finetune/outputs/qwen3.5-2b_pubmedvision
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 8
  warmup_steps: 100
  num_train_epochs: 1
  max_steps: -1
  learning_rate: 2e-4
  logging_steps: 10
  save_steps: 200
  eval_steps: 200
  optim: adamw_8bit
  weight_decay: 0.001
  lr_scheduler_type: cosine
  seed: 3407
  max_length: 8192
  bf16: true
  fp16: false
  dataloader_num_workers: 0
  report_to: none
